#include "main.h"

/**
 * _putchar - This function checks for uppercase letter
 * @c: inputted character
 *
 * Return: it returns 1 if c is uppercase 0 otherwise
 */
int _putchar(char c)
		{
		return (c);
		}
