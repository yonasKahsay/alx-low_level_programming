#include "main.h"

/**
 * mul - This function checks for uppercase letter
 * @a: first no
 * @b: second no
 *
 * Return: it returns their product
 */
int mul(int a, int b)
		{
		a = a * b;
		return (a);
		}
