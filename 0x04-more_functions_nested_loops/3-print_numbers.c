#include "main.h"

/**
 * print_numbers - This function checks for uppercase letter
 *
 * Return: it returns their product
 */
void print_numbers(void)
		{
			int i;

			for (i = 0 ; i < 10 ; i++)
			{
				_putchar(i + '0');
			}
		_putchar('\n');
		}
