#include <stdio.h>
int _abs(int n)
	{
	if (n > 0)
		{
		return (n);
		}
	else if (n < 0)
		{
		return (-n);
		}
	else
		{
		return (0);
		}
	}
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
return (0);
}
