#include <stdio.h>
#include "main.h"
int _islower(int c)
	{
	if (c < 123 && c > 96)
		putchar('\n');
		return (1);
	else
		putchar('\n');
		return (0);
	}
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
return (0);
}
