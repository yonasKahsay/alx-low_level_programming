#include "main.h"
#include <string.h>

/**
 * puts2 - This function print every other string
 * @str: inputted string
 *
 * Return: it returns nothing
 */
void puts2(char *str)
		{
		int i, j = 0;

		for (i = 0 ; i < 1000 ; i++)
		{
		if (((*(s + i)) == '\0' || *(s + i - 1)) == '\0')
		break;
		_putchar((*(s + i)));
		i = i + 2;
		}
		}
