#include "main.h"
#include <string.h>

/**
 * print_rev - This function prints a string
 * @s: inputted string
 *
 * Return: it returns nothing
 */
void print_rev(char *s)
		{
		int i;
		int j;

		for (i = 0 ; i < 1000 ; i++)
		{
		if ((*(str + i)) == '\0')
		break;
		}
		while (i > -1)
		{
			_putchar((*(str + i));
				i--;
		}
		 _putchar('\n');
		}
