#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char st[] = "\"Programming is like building a multilingual puzzle";/**string*/

puts(st);
return (0);
}
